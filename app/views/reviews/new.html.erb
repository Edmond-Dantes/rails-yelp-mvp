<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating do %>
    <%= f.select :rating, Review::RATINGS %>
  <% end %>
  <%= f.submit "Add this review", class: "btn btn-info" %>
<% end %>
